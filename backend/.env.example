# NestJS Application Settings
PORT=3000
NODE_ENV=development  # 'development' or 'production'

# Firebase Settings
FIREBASE_DATABASE_URL=https://your-firebase-project-id.firebaseio.com
FIREBASE_STORAGE_BUCKET=your-firebase-project-id.appspot.com

# CORS Settings
ALLOWED_ORIGINS=http://localhost:5173,https://your-production-domain.com

# Email Settings (Development Mode - Ethereal)
# In development mode, emails are sent to Ethereal (a fake SMTP service)
# The logs will display a preview URL to view the sent emails

# Email Settings (Production Mode)
# Uncomment and fill these settings for production use
# NODE_ENV=production
# EMAIL_HOST=smtp.ionos.com  # IONOS SMTP server
# EMAIL_PORT=465
# EMAIL_SECURE=true  # true for 465 port (SSL/TLS), false for other ports
# EMAIL_USER=your-email@yourdomain.com
# EMAIL_PASS=your-email-password
# EMAIL_FROM="Your Business <info@yourdomain.com>"
# EMAIL_REPLY_TO=info@yourdomain.com

# Alternative Email Providers:
# - SendGrid: EMAIL_HOST=smtp.sendgrid.net, EMAIL_PORT=587
# - Mailgun: EMAIL_HOST=smtp.mailgun.org, EMAIL_PORT=587
# - AWS SES: EMAIL_HOST=email-smtp.us-east-1.amazonaws.com, EMAIL_PORT=587
